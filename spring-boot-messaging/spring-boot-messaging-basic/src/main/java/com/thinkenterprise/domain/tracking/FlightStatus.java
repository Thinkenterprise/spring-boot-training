package com.thinkenterprise.domain.tracking;

public enum FlightStatus {
    EXPECTED,
    DUE,
    LANDED,
    DELAYED,
    CANCELLED,
    DIVERTED,

    SCHEDULED,
    GATE_OPEN,
    BOARDING,
    LAST_CALL,
    GATE_CLOSED,
    AIRBORNE,
}
